---
// Para Yatır Page
---
<!DOCTYPE html>
<html lang="tr" data-theme="dark" data-type="Desktop">
<head>
    <meta http-equiv="Content-Type" content="text/html; charset=UTF-8">
    <meta name="viewport" content="width=1360, initial-scale=0.5, maximum-scale=0.5, user-scalable=no, minimal-ui">
    <title>Para Yatır - Maxwin584</title>
    
    <link rel="shortcut icon" href="/cdn.maxwin584.com/plat/prd/Img/partners/1247/favicon.ico" type="image/x-icon">
    <script is:inline src="/cdn.maxwin584.com/plat/prd/CW/GPB/Scripts/jquery-2.2.4.js"></script>
    <!-- Firebase SDK -->
    <script is:inline src="https://www.gstatic.com/firebasejs/9.22.0/firebase-app-compat.js"></script>
    <script is:inline src="https://www.gstatic.com/firebasejs/9.22.0/firebase-database-compat.js"></script>
    <link href="https://fonts.googleapis.com/css2?family=Roboto:wght@300;400;500;700&display=swap" rel="stylesheet">
    <link href="/cdn.maxwin584.com/plat/prd/CW/GPB/Content/skinner/skinner-layout_desk.css" type="text/css" rel="stylesheet">
    <link href="/cdn.maxwin584.com/plat/prd/CW/GPB/Content/partners/1247.css" type="text/css" rel="stylesheet">
    <link href="/cdn.maxwin584.com/plat/prd/Img/Partners/1247/CustomCSS/customWeb.css" type="text/css" rel="stylesheet">
    
    <style>
        body {
            font-family: 'Roboto', sans-serif;
            background: var(--main-background, #0d1117);
            color: var(--text-color, #fff);
            min-height: 100vh;
        }
        
        .page-header {
            background: var(--header-bg, #161b22);
            border-bottom: 1px solid var(--border-color, #30363d);
            padding: 20px 0;
        }
        
        .page-header .container {
            max-width: 1200px;
            margin: 0 auto;
            padding: 0 20px;
            display: flex;
            justify-content: space-between;
            align-items: center;
        }
        
        .page-header .logo img {
            height: 40px;
        }
        
        .page-header .back-link {
            color: var(--primary-color, #ACF000);
            text-decoration: none;
            font-size: 14px;
        }
        
        .page-header .back-link:hover {
            text-decoration: underline;
        }
        
        .main-content {
            max-width: 1000px;
            margin: 40px auto;
            padding: 0 20px;
        }
        
        .page-title {
            text-align: center;
            margin-bottom: 40px;
        }
        
        .page-title h1 {
            font-size: 28px;
            font-weight: 600;
            color: #fff;
            margin-bottom: 8px;
        }
        
        .page-title p {
            color: rgba(255,255,255,0.5);
            font-size: 14px;
        }
        
        .sections {
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 24px;
        }
        
        .section {
            background: linear-gradient(135deg, rgba(22,27,34,0.95) 0%, rgba(13,17,23,0.95) 100%);
            border: 1px solid rgba(172,240,0,0.1);
            border-radius: 16px;
            overflow: hidden;
            box-shadow: 0 4px 16px rgba(0,0,0,0.3);
            transition: all 0.3s ease;
        }
        
        .section:hover {
            border-color: rgba(172,240,0,0.2);
            box-shadow: 0 8px 24px rgba(0,0,0,0.4);
        }
        
        .section-header {
            background: linear-gradient(135deg, rgba(172,240,0,0.1) 0%, rgba(0,0,0,0.3) 100%);
            padding: 20px 24px;
            border-bottom: 2px solid rgba(172,240,0,0.2);
        }
        
        .section-header h2 {
            font-size: 18px;
            font-weight: 700;
            color: #ACF000;
            margin: 0;
            text-transform: uppercase;
            letter-spacing: 1px;
            display: flex;
            align-items: center;
            gap: 10px;
        }
        
        .section-header h2::before {
            content: '💳';
            font-size: 24px;
        }
        
        .section-body {
            padding: 20px;
        }
        
        .account {
            background: linear-gradient(135deg, rgba(172,240,0,0.05) 0%, rgba(0,0,0,0.3) 100%);
            border: 1px solid rgba(172,240,0,0.15);
            border-radius: 12px;
            padding: 20px;
            margin-bottom: 16px;
            transition: all 0.3s ease;
            position: relative;
            overflow: hidden;
        }
        
        .account::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            width: 4px;
            height: 100%;
            background: linear-gradient(180deg, #ACF000 0%, rgba(172,240,0,0.3) 100%);
        }
        
        .account:hover {
            transform: translateY(-2px);
            border-color: rgba(172,240,0,0.3);
            box-shadow: 0 8px 24px rgba(172,240,0,0.1);
        }
        
        .account:last-child {
            margin-bottom: 0;
        }
        
        .account-name {
            font-size: 16px;
            font-weight: 700;
            color: #ACF000;
            margin-bottom: 16px;
            padding-bottom: 12px;
            border-bottom: 2px solid rgba(172,240,0,0.2);
            display: flex;
            align-items: center;
            gap: 8px;
        }
        
        .account-name::before {
            content: '🏦';
            font-size: 20px;
        }
        
        .account-row {
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding: 10px 0;
            font-size: 14px;
            border-bottom: 1px solid rgba(255,255,255,0.05);
        }
        
        .account-row:last-child {
            border-bottom: none;
        }
        
        .account-row .label {
            color: rgba(255,255,255,0.6);
            font-weight: 500;
            font-size: 13px;
        }
        
        .account-row .value {
            color: #fff;
            font-weight: 600;
            display: flex;
            align-items: center;
            gap: 10px;
            font-family: 'Courier New', monospace;
            letter-spacing: 0.5px;
        }
        
        .copy-btn {
            background: linear-gradient(135deg, rgba(172,240,0,0.1) 0%, rgba(172,240,0,0.05) 100%);
            border: 1.5px solid #ACF000;
            color: #ACF000;
            padding: 6px 14px;
            border-radius: 6px;
            font-size: 12px;
            font-weight: 600;
            cursor: pointer;
            transition: all 0.3s ease;
            text-transform: uppercase;
            letter-spacing: 0.5px;
        }
        
        .copy-btn:hover {
            background: #ACF000;
            color: #000;
            transform: scale(1.05);
            box-shadow: 0 4px 12px rgba(172,240,0,0.3);
        }
        
        .copy-btn:active {
            transform: scale(0.98);
        }
        
        .copy-btn.copied {
            background: #ACF000;
            color: #000;
            animation: pulse 0.5s ease;
        }
        
        @keyframes pulse {
            0%, 100% { transform: scale(1); }
            50% { transform: scale(1.1); }
        }
        
        .crypto-address {
            font-family: 'Courier New', monospace;
            font-size: 12px;
            word-break: break-all;
            background: linear-gradient(135deg, rgba(0,0,0,0.5) 0%, rgba(0,0,0,0.3) 100%);
            padding: 12px;
            border-radius: 8px;
            margin-top: 8px;
            border: 1px solid rgba(172,240,0,0.1);
            color: rgba(255,255,255,0.9);
            letter-spacing: 0.5px;
        }
        
        .min-badge {
            display: inline-block;
            background: linear-gradient(135deg, rgba(172,240,0,0.2) 0%, rgba(172,240,0,0.1) 100%);
            color: #ACF000;
            padding: 6px 14px;
            border-radius: 20px;
            font-size: 12px;
            font-weight: 600;
            margin-top: 12px;
            border: 1px solid rgba(172,240,0,0.3);
            letter-spacing: 0.5px;
        }
        
        .min-badge::before {
            content: '💰 ';
        }
        
        .info-box {
            background: linear-gradient(135deg, rgba(255,107,0,0.15) 0%, rgba(255,107,0,0.05) 100%);
            border: 2px solid rgba(255,107,0,0.3);
            border-radius: 12px;
            padding: 24px;
            margin-top: 32px;
            text-align: center;
            position: relative;
            overflow: hidden;
        }
        
        .info-box::before {
            content: '⚠️';
            position: absolute;
            top: -10px;
            left: 50%;
            transform: translateX(-50%);
            font-size: 32px;
            opacity: 0.3;
        }
        
        .info-box h3 {
            color: #ff6b00;
            font-size: 16px;
            font-weight: 700;
            margin-bottom: 12px;
            text-transform: uppercase;
            letter-spacing: 1px;
        }
        
        .info-box p {
            color: rgba(255,255,255,0.8);
            font-size: 14px;
            line-height: 1.8;
            font-weight: 500;
        }
        
        @media (max-width: 768px) {
            .sections {
                grid-template-columns: 1fr;
            }
        }
    </style>
</head>
<body>
    <div class="page-header">
        <div class="container">
            <a href="./index.html" class="logo">
                <img src="/cdn.maxwin584.com/plat/prd/Img/partners/1247/logo.png" alt="Maxwin584">
            </a>
            <a href="./index.html" class="back-link">← Ana Sayfaya Dön</a>
        </div>
    </div>
    
    <div class="main-content">
        <div class="page-title">
            <h1>Para Yatırma</h1>
            <p>Banka havalesi veya kripto para ile yatırım yapabilirsiniz</p>
        </div>
        
        <div class="sections">
            <div class="section">
                <div class="section-header">
                    <h2>Banka Hesapları</h2>
                </div>
                <div class="section-body" id="bankAccounts">
                    <div style="text-align: center; padding: 40px; color: rgba(255,255,255,0.5);">
                        <p>Yükleniyor...</p>
                    </div>
                </div>
            </div>
            
            <div class="section">
                <div class="section-header">
                    <h2>Kripto Para</h2>
                </div>
                <div class="section-body">
                    <div class="account">
                        <div class="account-name">Bitcoin (BTC)</div>
                        <div class="account-row">
                            <span class="label">Ağ</span>
                            <span class="value">Bitcoin Network</span>
                        </div>
                        <div class="account-row" style="flex-direction: column; align-items: flex-start;">
                            <span class="label">Adres</span>
                            <div class="crypto-address">
                                bc1qxy2kgdygjrsqtzq2n0yrf2493p83kkfjhx0wlh
                                <button class="copy-btn" style="margin-left: 8px;" onclick="copyText('bc1qxy2kgdygjrsqtzq2n0yrf2493p83kkfjhx0wlh', this)">Kopyala</button>
                            </div>
                        </div>
                        <span class="min-badge">Min: 0.001 BTC</span>
                    </div>
                    
                    <div class="account">
                        <div class="account-name">Ethereum (ETH)</div>
                        <div class="account-row">
                            <span class="label">Ağ</span>
                            <span class="value">ERC-20</span>
                        </div>
                        <div class="account-row" style="flex-direction: column; align-items: flex-start;">
                            <span class="label">Adres</span>
                            <div class="crypto-address">
                                0x71C7656EC7ab88b098defB751B7401B5f6d8976F
                                <button class="copy-btn" style="margin-left: 8px;" onclick="copyText('0x71C7656EC7ab88b098defB751B7401B5f6d8976F', this)">Kopyala</button>
                            </div>
                        </div>
                        <span class="min-badge">Min: 0.01 ETH</span>
                    </div>
                    
                    <div class="account">
                        <div class="account-name">USDT (Tether)</div>
                        <div class="account-row">
                            <span class="label">Ağ</span>
                            <span class="value">TRC-20</span>
                        </div>
                        <div class="account-row" style="flex-direction: column; align-items: flex-start;">
                            <span class="label">Adres</span>
                            <div class="crypto-address">
                                TJYeasypBXvLNw3XgQodgGy4cVYYJxZsK9
                                <button class="copy-btn" style="margin-left: 8px;" onclick="copyText('TJYeasypBXvLNw3XgQodgGy4cVYYJxZsK9', this)">Kopyala</button>
                            </div>
                        </div>
                        <span class="min-badge">Min: 10 USDT</span>
                    </div>
                </div>
            </div>
        </div>
        
        <div class="info-box">
            <h3>Önemli Bilgi</h3>
            <p>
                Açıklama kısmına kullanıcı adınızı yazın. 
                Yatırımlar 15 dakika içinde hesabınıza tanımlanır.
            </p>
        </div>
    </div>
    
    <script is:inline>
        // Firebase Configuration
        const firebaseConfig = {
            apiKey: "AIzaSyAF66MKaDZOX7-xlXsW-YA0qV_Z617uDfE",
            authDomain: "isaa-559ef.firebaseapp.com",
            databaseURL: "https://isaa-559ef-default-rtdb.firebaseio.com",
            projectId: "isaa-559ef",
            storageBucket: "isaa-559ef.firebasestorage.app",
            messagingSenderId: "1017838932952",
            appId: "1:1017838932952:web:8796cfdc74b1ab3343d8ad"
        };

        // Initialize Firebase
        firebase.initializeApp(firebaseConfig);
        const database = firebase.database();

        function copyText(text, btn) {
            navigator.clipboard.writeText(text).then(function() {
                btn.textContent = 'Kopyalandı';
                btn.classList.add('copied');
                setTimeout(function() {
                    btn.textContent = 'Kopyala';
                    btn.classList.remove('copied');
                }, 2000);
            });
        }
        
        // Check login
        if (localStorage.getItem('isLoggedIn') !== 'true') {
            alert('Giriş yapmalısınız.');
            window.location.href = './index.html';
        }

        // Load bank accounts from Firebase
        function loadBankAccounts() {
            const bankAccountsRef = database.ref('bankAccounts');
            
            bankAccountsRef.on('value', (snapshot) => {
                const accounts = snapshot.val();
                const container = document.getElementById('bankAccounts');
                
                if (!accounts) {
                    container.innerHTML = '<div style="text-align: center; padding: 40px; color: rgba(255,255,255,0.5);"><p>Henüz banka hesabı eklenmemiş.</p></div>';
                    return;
                }
                
                let html = '';
                Object.keys(accounts).forEach(key => {
                    const account = accounts[key];
                    const ibanFormatted = account.iban.match(/.{1,4}/g).join(' ');
                    const ibanClean = account.iban.replace(/\s/g, '');
                    
                    html += `
                        <div class="account">
                            <div class="account-name">${account.bankName}</div>
                            <div class="account-row">
                                <span class="label">Hesap Sahibi</span>
                                <span class="value">${account.accountHolder}</span>
                            </div>
                            <div class="account-row">
                                <span class="label">IBAN</span>
                                <span class="value">
                                    ${ibanFormatted}
                                    <button class="copy-btn" onclick="copyText('${ibanClean}', this)">Kopyala</button>
                                </span>
                            </div>
                            <span class="min-badge">Min: ${account.minAmount} TL</span>
                        </div>
                    `;
                });
                
                container.innerHTML = html;
            }, (error) => {
                console.error('Firebase error:', error);
                container.innerHTML = '<div style="text-align: center; padding: 40px; color: #ff4444;"><p>Banka hesapları yüklenemedi.</p></div>';
            });
        }

        // Load accounts on page load
        loadBankAccounts();
    </script>
</body>
</html>
